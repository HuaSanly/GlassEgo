# GlassEgo 仓库工作指南

## 适用范围与优先级

- 本文件适用于其所在目录及所有子目录。
- 本文件与上级目录的 `AGENTS.md` 共同生效；本文件负责补充 GlassEgo 仓库的具体要求。
- 系统指令、开发者指令和用户当前请求的优先级高于本文件。
- 若子目录存在更具体的 `AGENTS.md`，则该文件在其目录范围内优先。

## 核心原则

- **简洁优先**：每次变更尽量简单，只影响最少的代码。
- **绝不偷懒**：定位并解决根因，不使用临时修复，坚持资深开发者标准。
- **最小影响**：只修改必要部分，避免引入新的缺陷。

## 目录职责与边界

### 仓库根目录

- 根目录只存放仓库级说明、环境依赖和安装入口，例如 `AGENTS.md`、`requirements.txt` 和 `setup.sh`。
- 业务实现应放入所属子系统，不要在根目录堆放临时脚本、模型逻辑或生成产物。
- 修改依赖时同步核对 `requirements.txt` 与 `setup.sh`，避免安装说明和实际运行环境继续分化。

### `.vscode/`

- 仅存放本地编辑器配置，不得成为程序运行所必需的配置来源。
- 不要在其中保存凭据、机器专属路径或需要提交的业务配置。

### `data/`

- 作为原始输入和预处理产物的运行时数据根目录，不存放源代码。
- 每个一级子目录代表一个独立处理单元，例如 `data/1/`。
- 当前每个处理单元只允许包含一个直属视频文件；可附带 `poses.json`、`pose.json` 或 `camera_poses.json`。
- 姿态文件当前只会被扫描并登记，尚未接入相机位姿或世界坐标计算。
- 预处理产物写入同一单元的 `preprocess/` 子目录，包括逐帧 JSON、分析图和可视化视频。
- 数据集、视频、模型产物和其他大文件保持 Git 不跟踪；未经用户明确要求，不删除或覆盖已有数据单元。

### `datacollection/`

- 负责原始传感器数据采集、设备接入和采集格式封装；当前目录及 `rokidglass3/` 尚无源文件，仅作为后续实现的占位。
- 设备专属实现放入对应子目录，例如 `datacollection/rokidglass3/`，不要把设备分支散落到通用预处理代码中。
- 采集模块只负责生成可追溯的原始数据单元，不执行手部推理、离线优化或训练。
- 新采集结果应遵循 `data/<unit>/` 布局，并保留原始时间戳和标定信息。

### `preprocess/`

- 负责离线预处理流程，包括扫描数据单元、构建传感器数据、调用处理模块和组织输出。
- `test_pipeline.py` 当前是预处理 CLI 与协调入口，尽管名称包含 `test`，它不属于自动化测试。
- 流程入口只负责编排和边界检查；具体模型、数据结构和通用工具应留在各自模块中。
- 不要在此目录实现实时数据采集或训练循环。

#### `preprocess/config/`

- 作为整个预处理系统的配置来源，目前由 `default.yaml`、`sensors.yaml` 和 `hand_tracking.yaml` 组成。
- `default.yaml` 管理全局运行路径和输出开关，`sensors.yaml` 管理传感器参数，`hand_tracking.yaml` 管理手部处理参数。
- 只添加代码中实际读取的配置项；删除功能时同步删除失效配置，避免无效参数。
- 不保存凭据、绝对机器路径、数据样本或模型权重。

#### `preprocess/data_types/`

- 定义预处理阶段共享的数据结构、坐标数据和序列化逻辑，目前主要包括 `Cam`、`CamData`、`Hands` 和相关手部类型。
- 数据类型模块不得初始化模型、下载权重或扫描数据根目录。
- 修改字段、坐标含义或 JSON 结构时，必须同步检查生产者、消费者和已有输出兼容性。
- 与图像有关的数据类型必须明确颜色空间、形状、单位和时间戳单位。

#### `preprocess/hand_tracking/`

- 负责手部检测、HaMeR 三维恢复、轨迹后处理、运动学优化、可视化和分析。
- 检测器与模型封装只处理单帧或单个裁剪，不负责扫描 `data/`、加载全局配置或决定批处理策略。
- `HaMeRHandsGenerator` 负责单视频手部流程协调；`HandsTrajectoryOptimizer` 负责轨迹优化；`HandsOps` 负责可视化、统计和分析操作。
- 21 点顺序统一使用 Generator 声明的 HaMeR/OpenPose 顺序；修改索引时必须同步所有生产、优化、序列化和可视化代码。
- 模块应通过传入的 `Cam`、配置对象和单元目录工作，不自行推断仓库根路径。

### `training/`

- 负责训练、验证和训练侧评估；当前目录尚未形成可运行实现。
- 训练代码消费 `data/<unit>/preprocess/` 下的稳定产物，不应反向承担数据采集或预处理推理职责。
- 训练检查点、日志和实验产物不得提交到源码目录，除非它们是明确要求的小型测试夹具。

### `utils/`

- 存放可被多个模块复用的数学、媒体和可视化辅助函数，目前包括 `utils_math.py`、`utils_media.py` 和 `utils_vis.py`。
- 工具函数应保持职责单一、显式输入输出，并尽量避免隐藏状态。
- 手部专属算法、批处理编排和模型生命周期逻辑必须留在 `preprocess/hand_tracking/`，不要下沉到 `utils/`。
- 新增工具前先确认存在真实复用需求；避免为了单次调用创建通用抽象或引入循环依赖。

### 目标数据流

```text
datacollection/ -> data/<unit>/ -> preprocess/ -> data/<unit>/preprocess/ -> training/
```

- 当前只有数据单元扫描和手部预处理已经形成实现；采集端与训练端尚未贯通。
- 配置由预处理入口统一加载，再通过参数传给下游模块；下游模块不要重复读取全局 YAML。
- 共享数据契约集中在 `preprocess/data_types/`，生产者与消费者不得各自维护不一致的字段定义。
- 所有生成文件、缓存目录和 `__pycache__/` 都不属于源码，不要手动编辑或新增到版本控制；仓库中已经被跟踪的历史缓存应另行清理，不在无关任务中顺手删除。

## 工作流

### 1. 默认使用计划节点

- 任何非琐碎任务，包括 3 步以上的任务或架构决策，都必须先制定并维护计划。
- 验证步骤也必须纳入计划，而不仅用于实现阶段。
- 提前写清规格、成功标准和验证检查点，减少歧义。
- 一旦实际情况偏离计划，立即停止并重新规划，不要继续硬推。

多步骤任务使用以下格式：

```text
1. [步骤] -> 验证：[检查点]
2. [步骤] -> 验证：[检查点]
3. [步骤] -> 验证：[检查点]
```

### 2. 子代理策略

- 大量使用子代理，保持主上下文窗口整洁。
- 将研究、探索和可并行分析交给子代理处理。
- 复杂问题应通过子代理投入更多算力。
- 每个子代理只负责一个明确方向。

### 3. 先思考，再编码

- 不要假设，也不要隐藏困惑；明确说明假设、疑问和取舍。
- 实现前若存在不确定项，先询问用户。
- 如果存在多种解释，全部列出，不要默默选择。
- 如果存在更简单的方法，应主动说明；有充分理由时提出反对意见。
- 如果需求不清楚，停止实现，指出具体歧义并提问。

### 4. 目标驱动执行

- 开始前定义成功标准，并持续执行到验证通过。
- 将任务转化为可验证目标，例如：
  - “增加验证” -> 先为无效输入编写测试，再让测试通过。
  - “修复 bug” -> 先编写能够复现问题的测试，再让测试通过。
  - “重构 X” -> 确保重构前后的测试都通过。

## 实现规范

### 1. 简单优先

- 用最少的代码解决问题，不编写投机性代码。
- 不实现需求范围之外的功能。
- 不为一次性使用的代码创建抽象。
- 不添加未被要求的灵活性或可配置性。
- 不为不可能发生的场景编写错误处理。
- 如果 200 行代码可以缩减到 50 行，应重写为更简单的实现。
- 随时检查：资深工程师是否会认为当前实现过于复杂？如果是，就简化。

### 2. 手术式修改

- 只修改完成当前任务所必需的部分，只清理自己造成的问题。
- 编辑现有代码时：
  - 不要顺便改进相邻代码、注释或格式。
  - 不要重构没有问题的代码。
  - 匹配现有代码风格，即使你个人偏好不同。
  - 可以报告无关的死代码，但不要擅自删除。
- 当本次变更产生未使用项时：
  - 删除因本次变更而未使用的导入、变量和函数。
  - 除非用户明确要求，否则不要删除此前已经存在的死代码。
- 每一行修改都必须能够直接追溯到用户请求。

### 3. 要求优雅，但保持平衡

- 非琐碎改动前暂停确认：是否存在更优雅的方案？
- 如果实现显得临时或拼凑，应基于当前掌握的信息重新设计。
- 简单问题不要过度工程化。
- 每次提交结果前，重新审视实现是否足够简单、清晰和可靠。

### 4. 代码风格与复用

- 优先遵循用户和仓库现有的代码风格。
- 尽量保持模块化、高内聚和低耦合。
- 如果现有风格存在明显问题，应及时说明并给出修改建议。
- 优先复用成熟开源方案和仓库已有能力，不重复造轮子。

## 验证与测试

- 验证范围应与改动风险相匹配，并纳入任务计划。
- 未修改且已确认正确的代码不需要额外测试，以减轻工作流程。
- 如果需要编写临时测试代码，将其放在仓库对应目录下，并确保 Git 不跟踪这些测试文件，避免污染仓库。
- 完成前确认实现、验证结果和已知限制都已明确说明。
