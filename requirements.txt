# ==============================================================================
# HumanEgo — 核心依赖项
# ==============================================================================
# 安装方式： pip install -r requirements.txt
# 要进行完整设置（包括手部追踪），请使用：SKIP_HAND=0 bash setup.sh
# ==============================================================================

# ── PyTorch — 固定到 CUDA 12.x 版本（适用于驱动程序 >= 525 / CUDA 12.x） ──
# 注意：未固定的 `torch>=2.5` 会解析为最新的 cu13x wheel，该版本在
# 常见的 CUDA 12.x 驱动程序 (torch.cuda.is_available() == False）。较新的 GPU（例如
# Blackwell）可能需要更新的 cu12x/cu13x 版本 - 调整版本 + 索引以匹配
# 您的驱动程序（请参阅https://pytorch.org）。安装后验证：
#   python -c "import torch; print(torch.cuda.is_available())"
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
xformers>=0.0.28

# ── 科学计算 ──
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.21
pandas>=2.0

# ── 计算机视觉 ──
opencv-python>=4.8
pillow>=10.0
open3d>=0.18
imageio>=2.30
imageio-ffmpeg>=0.5
pyrender>=0.1.45
trimesh>=4.0

# ── 深度学习模型 ──
transformers>=4.40,<5    # 5.x 与 Grounding DINO 的 AutoProcessor 不兼容（DINO-SAM 预处理阶段）
sam2>=1.0
diffusers>=0.25
timm>=0.9
einops>=0.7
rembg>=2.0
onnxruntime-gpu>=1.17
ultralytics>=8.3
filterpy>=1.4
ffmpeg-python>=0.2

# ── 3D / 位姿 ──
roma>=1.4
pyquaternion>=0.9
smplx>=0.1.28

# ── Project Aria & 机器人硬件 ──
# 注意：projectaria-client-sdk 1.1.0 强制依赖 projectaria-tools==1.5.2a1，
# 但本流水线需要 1.7.1。setup.sh 使用 --no-deps 安装两者，因此 pip 会打印
# 关于该版本约束的无害解析器冲突警告。termcolor 是 client-sdk CLI 的
# 依赖项；在这里列出它可以消除“termcolor 未安装”这部分解析器警告。
projectaria-tools==1.7.1
projectaria-client-sdk==1.1.0
termcolor>=2.0
pyrealsense2>=2.50
trossen-arm>=1.8

# ── 配置 ──
omegaconf>=2.3
hydra-core>=1.3
pyyaml>=6.0
python-box>=7.0
pydantic>=2.0

# ── 机器学习操作与数据 ──
huggingface-hub>=0.23
wandb>=0.16
accelerate>=0.25

# ── 公用事业 ──
tqdm>=4.65
rich>=13.0
python-dotenv>=1.0
requests>=2.31
safetensors>=0.4
rosbags>=0.11

# ── 可视化 ──
matplotlib>=3.7
seaborn>=0.13
plotly>=5.15
rerun-sdk>=0.18

# ── 网页 / 演示 ──
gradio>=5.0
